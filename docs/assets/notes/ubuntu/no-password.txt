simba ALL=(ALL) NOPASSWD:ALL

sudo passwd -d `whoami`

sudo passwd -d simba

soooo you have to use the terminal and not a gui if you want a password again, so its basically useless as you cant really do sudo or anything practical

sudo passwd simba